# --- Postgres ---
POSTGRES_USER=app
POSTGRES_PASSWORD=changeme
POSTGRES_DB=fitness

# --- API ---
DATABASE_URL=postgresql+asyncpg://app:changeme@postgres:5432/fitness
JWT_SECRET=replace-me-with-a-256-bit-base64-secret
ENV=development

# Чтобы PWA на 5173 ходил на API без CORS-проблем, оставляем дефолт.
# CORS_ORIGINS=["http://localhost:5173","http://127.0.0.1:5173"]
