"""Top-level pytest config: shared fixtures live here.

For now intentionally empty — fixtures will be added when first needed
(don't speculate, don't pre-build infra; let tests pull what they require).
"""
